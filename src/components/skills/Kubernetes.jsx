import BaseSkill from './BaseSkill';

const Kubernetes = () => {
    return (
        <BaseSkill 
            name="Kubernetes"
            color="#326CE5"
            docsUrl="https://kubernetes.io/docs/home/"
        />
    );
};

export default Kubernetes; 